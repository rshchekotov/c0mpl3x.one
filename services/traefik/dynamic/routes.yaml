http:
  routers:
    dashboard:
      rule: "Host(`dashboard.c0mpl3x.one`)"
      entryPoints:
        - websecure
      service: api@internal
      tls: {}

    whoami:
      rule: "Host(`whoami.c0mpl3x.one`)"
      entryPoints:
        - websecure
      service: whoami
      tls: {}

  services:
    whoami:
      loadBalancer:
        servers:
          - url: "http://whoami:80"
